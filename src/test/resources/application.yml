spring:
  main:
    banner-mode: off
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: test
    password: test

logging:
  level:
    root: INFO
    org.springframework.amqp: DEBUG
    org.glassfish.jersey: DEBUG
    net.thumbtack: DEBUG

todoist:
  uri: https://api.todoist.com/sync/v8/sync

db:
  namespace: ptpb-wrapper
  port: 3000
  host: localhost
